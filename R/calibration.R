calibration <- function(
    file = NULL,
    channel = "left",
    from = 0,
    to = Inf,
    weighting = "none",
    ref = 20,
    leqsignal = NA
){
    # usar leqbands em dBFS
    # pegar Leq
    # comparar com o valor de referência (leqcalib)
    # calcular o valor de calibração
}